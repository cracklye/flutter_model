part of flutter_model; 



/// The visibility filter allows for a code based
/// filter to exclude records in the model from the UI interface.
/// This would normallly be used when searching.  This implementation will return true for all models unless the isVisible method is overridden.
// class FunctionalFilter<T extends IModel> extends Filter<T>{
//   final bool Function(T) filter; 
//   FunctionalFilter(this.filter);
// }
